In this example executable which is using  static lib which is using shared library

So, steps are:
 1. Create shared library
 2. Link shared library into the static
 3. Link static lib into the main executable
